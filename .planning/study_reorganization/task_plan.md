# 任务计划：D:\study 全目录整理

## 目标

重新审计并整理整个 `D:\study`。保留 `.git`、`.obsidian`、`.agents`、`.planning`、`.worktrees` 等基础设施原位；对 `408`、`数学一`、`英语一` 等学习资料建立统一、稳定、适合 Obsidian 使用的目录架构，并修复移动造成的本地链接。

## 原则

1. 不使用 `superpower / using-superpowers` skill。
2. 不删除重复文件；只生成重复项清单，需删除时另行确认。
3. 不移动 Git、Obsidian、规划记录和工作树基础设施。
4. 移动前验证源路径与目标路径都位于 `D:\study`，且目标不存在冲突。
5. 所有移动记录到 `move-log.md`，完成后核对文件数量与总字节数。
6. 外部移动后主动修复 Obsidian Wikilink、Markdown 相对路径和导航入口。

## 阶段

### 阶段 1：全量盘点

- [x] 统计全部目录、文件类型、大小和层级
- [x] 区分基础设施与学习资料
- [x] 检测同名、同大小和同哈希重复项
- **状态：** complete

### 阶段 2：目标架构与移动映射

- [x] 设计全局根目录顺序和各学科内部层级
- [x] 形成逐项源路径 → 目标路径映射
- [x] 检查覆盖、重名、超长路径和链接影响
- **状态：** complete

### 阶段 3：执行整理

- [x] 执行目录重命名与文件归位
- [x] 生成根目录入口和各学科导航
- [x] 保留时间戳并记录移动日志
- **状态：** complete

### 阶段 4：链接修复

- [x] 更新全库 Wikilink
- [x] 更新 Markdown 相对路径、PDF 页码入口与导航
- [x] 排查旧路径残留
- **状态：** complete

### 阶段 5：验证

- [x] 核对学习资料文件数与总字节数
- [x] 检查断链、歧义链接和配置完整性
- [x] 输出最终架构、重复项清单和使用说明
- **状态：** complete

## 错误记录

| 日期 | 错误 | 次数 | 处理 |
|---|---|---:|---|
| 2026-07-15 | 重复文件审计命令把 `Sort-Object Count -Descending,Name` 写成了无效参数组合 | 1 | 改用显式属性哈希表分别指定 `Count` 降序、`Name` 升序后重跑 |
| 2026-07-15 | 一次组合盘点命令最终返回 1，原因是 `rg | Select-Object -First` 提前关闭管道；前面的读取输出完整 | 1 | 后续将 `rg` 结果先收集再截取，避免管道提前关闭；未发生文件修改 |
| 2026-07-15 | 移动预检命令再次在闭合的 `foreach` 后直接接管道，触发“空管道元素” | 1 | 改为先把循环结果保存到 `$rows`，再单独格式化输出；未执行移动 |
| 2026-07-15 | 旧路径计数命令复用了同类 `foreach | Format-Table` 写法，再次触发解析错误 | 1 | 所有后续循环统一先赋给数组变量再输出；目录移动已完成且不受本次只读命令影响 |
| 2026-07-15 | 首轮 Wikilink 验证把表格中的转义别名分隔符 `\|` 误当成目标末尾反斜杠，误报 22 条计组链接 | 1 | 解析别名前先把 `\|` 规范为 `|`，再重跑；结构、旧路径和 JSON 检查已通过 |
| 2026-07-15 | 完成前全量验证发现线代 `06-特征值与特征向量.md` 的展示公式分隔符为奇数 | 1 | 按系统化调试定位具体未闭合公式块，最小修复后重跑全量验证 |
| 2026-07-15 | 首次公式定位命令同时触发 PowerShell 的 `$i:` 变量歧义和循环后直连管道解析错误 | 1 | 改为 `${i}:` 并先收集邻章统计数组；命令在解析阶段失败，未修改笔记 |
