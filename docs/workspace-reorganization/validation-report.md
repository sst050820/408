# D:\study 工作区重构验证报告

验证日期：2026-07-13  
当前阶段：全部完成

## 基线

| 区域 | 文件数 | 字节数 | 状态 |
|---|---:|---:|---|
| `408-考研` | 42 | 940,935,638 | recorded |
| `考研/408` | 186 | 2,586,102,516 | recorded |
| `考研/数一` | 87 | 791,981,889 | recorded |
| `考研/英一` | 88 | 269,547,890 | recorded |
| `ocr_output` | 1,444 | 8,581,920 | recorded |
| 根目录王道 PDF | 4 | 551,193,350 | recorded |
| Python 缓存 | 1 | 5,102 | recorded |

逐文件 SHA-256 基线：1,815 条，合计 5,147,972,590 字节。清单为 `baseline-hashes.csv`。

## 阶段结果

| 阶段 | 验证内容 | 状态 |
|---|---|---|
| 基线 | 路径、数量、字节数、SHA-256 | pass |
| 目标骨架与缺失 XMind | 创建 24 个目标目录；恢复 1,298,762 字节 XMind，ZIP/文件哈希一致 | pass |
| 408 迁移 | 192 项迁入；191 项基线哈希 + 1 项恢复哈希全部一致；旧解压来源文件为 0 | pass |
| 数学一迁移 | 87 份 PDF、791,981,889 字节；基线哈希不匹配 0；旧目录文件 0 | pass |
| 英语一与 CET-6 迁移 | 英语一 21 份、CET-6 67 份，共 269,547,890 字节；哈希不匹配 0 | pass |
| OCR 工程 | 1,444 个输出、8,581,920 字节，基线哈希不匹配 0；脚本移除明文凭据，静态安全检查通过 | pass |
| Obsidian 索引与路径 | 新建 4 份索引、212 个 Wikilink，未解析 0；有效旧 OCR 路径 0；workspace JSON 合法且 4 份 PDF 路径已更新 | pass |
| 删除前验收 | 38 份主笔记/26 章结构问题 0；79 份活动笔记、487 个真实 Wikilink 未解析 0；3 个 ZIP 共 184/184 条目哈希匹配；分科数量与字节数一致 | pass |
| 删除后验收 | 38 份主笔记/26 章结构问题 0；79 份活动笔记、487 个真实 Wikilink 未解析 0；OCR 凭据命中 0；旧路径与删除目标均不存在 | pass |

## 删除后结果

- 408 主笔记：38 份 Markdown、26 章；Frontmatter、必需标题、围栏、LaTeX 与占位符问题为 0。
- 活动资料笔记：79 份 Markdown、487 个真实 Wikilink；排除 3 个模板变量后未解析为 0。
- 数学一：87 份 PDF、791,981,889 字节；英语一：21 份 PDF；CET-6：67 份 PDF；英语资料合计 269,547,890 字节。
- OCR：1,444 个原输出文件、8,581,920 字节；类密钥文本和硬编码 `API_KEY` 赋值均为 0。
- 王道 2026：带书签阅读版 4 份、OCR 源 PDF 4 份，共 8 份。
- 恢复的网络层 XMind SHA-256 仍为 `6E3B1268DA094564853EA1E74D4730A95111198258D3EB0BFD41F86E26B20EEB`。
- `.obsidian/workspace.json` 可解析；活动文档中的旧 `ocr_output/` 路径命中为 0。
- 三个重复 ZIP、一个 Python 缓存和七个旧空目录均不存在；实际释放 1,266,481,495 字节。
- 过程记录 `task_plan.md`、`findings.md`、`progress.md` 已归档到 `docs/workspace-reorganization`，根目录对应文件均不存在。
- 最终三组独立只读验收均为 `PASS`：目录/状态、内容/安全、Markdown/链接。

## 环境说明

- 执行初期 worktree 检测曾因 Windows 沙箱 ACL 返回错误，因此本次没有创建隔离 worktree 或 Git 检查点，改用逐文件 SHA-256 与阶段报告隔离。
- 最终只读检测可识别为普通 Git 仓库，当前分支为 `main`、不是 linked worktree；本次未执行提交、合并、推送或 Git 清理。
- 当前系统无 `python`、`py`、`pdfinfo`，OCR 脚本只做静态验证。
- OCR 脚本安全替换后 SHA-256 为 `EE711DA95F6801F4D11360CA67106500D0F958C3BD8F12FA023CB7A67174FC74`；旧凭据仍需在服务端撤销并轮换。
