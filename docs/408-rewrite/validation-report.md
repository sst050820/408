# 408 Obsidian 重写验证报告

## 基线

验证时间：2026-07-13。

| 项目 | 数量 | 字节数 | 状态 |
|---|---:|---:|---|
| `408-考研` Markdown | 32 | 856,555 | 已记录 |
| `408-考研复习` Markdown | 35 | 98,923 | 已记录 |
| `ocr_output` TXT | 1,444 | 8,581,920 | 已记录 |

## 环境

- 工作目录：`D:\study`
- Obsidian 配置目录：`D:\study\.obsidian`
- Git：当前目录不是 Git 仓库，无法建立 worktree 或提交检查点。
- 隔离策略：各分科代理只写入各自科目目录；主代理负责共享总览、模板、迁移和删除。

## 阶段验证

| 阶段 | 文件数 | Frontmatter 缺失 | 必需章节缺失 | 未解析链接 | 结构错误 | 结果 |
|---|---:|---:|---:|---:|---:|---|
| 基线 | 67（两库合计） | 9（主库章节） | 未检查 | 未检查 | 未检查 | 已记录 |
| 总览与模板 | 8 | 0 | 0 | 待全库检查 | 0 | pass |
| 数据结构 | 9 | 0 | 0 | 0 | 0 | pass（规格与质量复核通过） |
| 计算机组成原理 | 8 | 0 | 0 | 0 | 0 | pass（规格与质量复核通过） |
| 操作系统 | 6 | 0 | 0 | 0 | 0 | pass（规格与质量复核通过） |
| 计算机网络 | 7 | 0 | 0 | 0 | 0 | pass（规格与质量复核通过） |
| 全库删除前 | 38 | 0 | 0 | 0 | 0 | pass（26 章、323 个链接） |
| 全库删除后 | 38 | 0 | 0 | 0 | 0 | pass（26 章、323 个链接） |

## 删除门槛

- [x] 26 个章节全部重写。
- [x] 39 项迁移映射（35 份 Markdown、4 份 PDF）全部为 `verified`。
- [x] 全部 Wikilink 可解析；模板示例链接不计入目标解析。
- [x] UTF-8、Frontmatter、标题、围栏、LaTeX 和 Callout 检查通过。
- [x] 删除目标绝对路径严格等于 `D:\study\408-考研复习`。
- [x] 目标目录中不存在未登记的非 Markdown 文件；4 份独有 PDF 已迁移，5 份子库配置按方案不合并。

## 最终结果

- 删除前：`markdown=38 chapters=26 links=323 unresolved=0 issues=0`。
- 迁移清单：`verified=39 pending=0`；带书签 PDF：4 份。
- 删除后：`markdown=38 chapters=26 links=323 unresolved=0 issues=0`。
- 重构后路径状态：`408-考研复习=False`、`408-考研=True`、历史 `ocr_output` 已迁入 `408-考研/98-OCR工程/output`；历史 `考研/408` 的解压内容已迁入资料库，仅保留待删除的重复外层 ZIP。
- 独立终审：`APPROVED`，未发现阻止交付的问题。

## 修正记录

- 2026-07-13：计划原使用 `rg -L` 表示“未匹配文件”，但 ripgrep 中 `-L` 是 `--follow`。已统一改为 `rg --files-without-match` 并复验总览文件，缺失 `status: rewritten` 为 0。
- 2026-07-13：首版 Wikilink 预检脚本误选 `String.Replace(char,char)` 重载，首次“未解析为 0”作废；第二版又因 `Path.GetFileName` 无法处理部分 Obsidian 目标字符而作废。第三版改用显式字符串替换与 `/` 分段解析，稳定发现 2 个真实旧链接 `../408考研笔记总览`，待操作系统修复。
- 2026-07-13：删除前检查发现旧库含 4 个带书签 PDF 和 5 个 `.obsidian` 配置。四个 PDF 与根目录同科 PDF 大小均不同，改为迁入保留库；旧子库配置不迁移。
- 2026-07-13：一次 PDF 大小比较命令因压缩语句导致 PowerShell 空管道语法错误，无结果；恢复可读写法后成功比较。
- 2026-07-13：一次全库验证命令因压缩掉 `foreach ... in ...` 的必要空格而语法失败，无结果；恢复可读脚本后验证为 `markdown=38 chapters=26 links=323 unresolved=0 issues=0`。
- 2026-07-13：一次删除前复验误用了未在设计中的 Frontmatter 字段，并将模板示例链接当作真实目标，结果作废；按设计规定的 10 字段及模板排除规则复验通过。
- 2026-07-13：允许同义章节标题后的首个压缩版复验命令再次因 `foreach ... in ...` 空格缺失而语法失败，无结果；可读版本复验通过。
